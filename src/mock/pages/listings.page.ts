import { mockAds, mockCategories, mockListings, mockStories } from "../shared";

export const listingsPageFallback = {
  listings: mockListings,
  categories: mockCategories,
  stories: mockStories,
  ads: mockAds,
};
